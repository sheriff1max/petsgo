# Описание проекта

Напиши incRacy(counter *int, n int): n горутин делают (*counter)++ без защиты. Запусти go run -race — увидишь предупреждение и, скорее всего, итог меньше n. Затем тип Counter с sync.Mutex: методы Inc() и Value() с блокировкой — гонка исчезает, итог всегда точный.
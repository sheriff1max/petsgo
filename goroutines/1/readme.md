# Описание проекта

parallelSquares(n int) []int: создай срез длины n, запусти n горутин, каждая записывает свой квадрат в свой индекс (это безопасно без мьютекса — объясни почему), sync.WaitGroup ждёт завершения.
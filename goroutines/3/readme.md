# Описание проекта

generate(n int) <-chan int: горутина отправляет 1..n и закрывает канал. В main обойди через for range — цикл сам завершится на close. Убедись, что без close диапазон-цикл зависает (проверь на черновике!).